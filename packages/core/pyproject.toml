[project]
name = "workspace-core"
version = "1.0.0"
description = "Core shared utilities package"
requires-python = ">=3.10"

dependencies = [
    "pydantic>=2.0.0",
    "python-dateutil>=2.8.0",
    "requests>=2.31.0",
    "typing-extensions>=4.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"