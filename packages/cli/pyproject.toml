[project]
name = "workspace-cli"
version = "1.0.0"
description = "CLI tool package"
requires-python = ">=3.10"

dependencies = [
    "workspace-core",
    "click>=8.1.0",
    "rich>=13.0.0",
    "typer>=0.9.0",
]
