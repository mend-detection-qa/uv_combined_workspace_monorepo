[project]
name = "workspace-api"
version = "1.0.0"
description = "API server package"
requires-python = ">=3.10"

dependencies = [
    "workspace-core",
    "fastapi>=0.109.0",
    "uvicorn>=0.27.0"
]
